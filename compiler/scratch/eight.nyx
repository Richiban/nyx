type Person = (
    firstName: string
    surname: string
)

def Person.fullName: Person -> string = { p ->
    "{p.firstName} {p.surname}"
}

def A.a = { s -> s }

def p: Person = (
    firstName = "",
    surname = ""
)

def a = aiei

def n = p \ Person.fullName

def n2 = p \ fullName