module one

def f = { x ->
    match x
    | "hi" -> "bye"
    | "one" -> "two"
}

def g = {
    | "hi" -> "bye"
    | "one" -> "two"
}

def f2 : (#a | #b) -> string = {
    | #a -> "got a"
    | #b -> "got b"
}

def sum: list(int) -> int = {
    | [] -> 0
    | [head, ...tail] -> 1
}

def main = {
    dbg(sum([1, 2, 3]))
}

main()