def factorial = { n ->
    if n <= 1
    then 1
    else n * factorial(n - 1)
}

def fib = { n ->
    if n <= 1
    then n
    else fib(n - 1) + fib(n - 2)
}

def main = {
    def f5 = factorial(5)
    def fib10 = fib(10)
    f5 + fib10
}
