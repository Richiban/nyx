
type Person = (
  name: string
  age: int
  score: int
)

def printPerson: Person -> () = { p ->
  dbg(p.name)
  dbg(p.age)
  dbg(p.score)
  ()
}

def main = {
  def p = (name = "Alice", age = 30, score = 100)
  printPerson(p)

  def p2 = Person(name = "Bob", age = 25, score = 80)
  printPerson(p2)
}