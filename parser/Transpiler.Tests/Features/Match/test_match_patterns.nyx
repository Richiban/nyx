module MatchPatterns

def withGuard = match value
	| > 5 -> 1
	| else -> 0

def listMiddle = match values
	| [1, ..., 3] -> 1
	| else -> 0

def recordMember = match point
	| (x = 1, y = y) -> y
	| else -> 0

def recordPositional = match point
	| Point(1, y) -> y
	| else -> 0
