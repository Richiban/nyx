
def f: int -> string = {
    | 1 -> "one"
    | _ -> "many"
}

context Console = (
    println: string -> ()
)

context CAdd = (
    add: (int, int) -> int
)

def g: [Console + CAdd] = {
    println("Hello world")
    def r = add(4, 5)
}

def h = {
    use Console(
        println = { s -> () }
    )

    use CAdd(
        
        add = { x, y -> x + y }
    )

    g()
}