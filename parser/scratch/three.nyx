

def f: (x: int, y: int) -> string = { x, y ->
    ""
}

def args = (x = 4, y = 5, z = 0)

f(x = 4, y = 5)
f(args)

def u: #some(string) | #nil = #nil

def g: (#some(string) | #nil | #notFound) -> string = { o -> 
    ""
}

g(u)

def mapOption: ((#some(a) | r), a -> b) -> #some(b) | r = {
    | #some(v), f -> #some(f(v))
    | other, _ -> other
}

def l: #some(string) | #nil | #invalid = #some("")

def l2 = l \mapOption { s -> 1 }