// Module: TestTranspile
const x = 42;
const y = "hello";
const sum = (1 + 2);
const point = { x: 10, y: 20 };
const accessField = point.x;
const tuple = [1, 2, 3];
const nested = outer.inner.value;
const ifTest = (true ? 1 : 2);
const funcCall = add(5, 10);
const piped = double(value);